# Base URL of the FastAPI backend (no trailing slash, no /api suffix — the client appends /api)
# Local dev: http://localhost:8001
# Production:  https://realtorspal-api.onrender.com
REACT_APP_BACKEND_URL=http://localhost:8001
